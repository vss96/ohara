{"id": "retry_backoff_rust", "query": "retry with exponential backoff", "language": "rust", "since_unix": null, "expected_commit_labels": ["retry_backoff_commit"], "expected_paths": ["src/fetch.rs"], "expected_profile": "unknown", "notes": "Plan 10 anchor: the canonical 'find_pattern' demo. Rank-1 must be the commit that introduced the retry/sleep loop, not the unrelated initial-fetch or login commits."}
{"id": "timeout_handling_rust", "query": "request timeout handling", "language": "rust", "since_unix": null, "expected_commit_labels": ["timeout_commit"], "expected_paths": ["src/fetch.rs"], "expected_profile": "unknown", "notes": "A separate commit introduces a per-request timeout. Tests that BM25-on-diff-text + symbol-name lanes both lift the timeout commit above the unrelated retry/login changes."}
{"id": "config_loading_python", "query": "load configuration from environment", "language": "python", "since_unix": null, "expected_commit_labels": ["config_loader_commit"], "expected_paths": ["app/config.py"], "expected_profile": "configuration", "notes": "Cross-language sanity: an env-var config loader added in Python. Verifies the Python tree-sitter chunker feeds the symbol lane and the language filter narrows hits."}
{"id": "error_wrapping_rust", "query": "wrap errors with context", "language": "rust", "since_unix": null, "expected_commit_labels": ["error_context_commit"], "expected_paths": ["src/error.rs"], "expected_profile": "unknown", "notes": "Picks up an `anyhow!` / `with_context` style change. Tests that semantic phrasing ('wrap', 'context') hits the diff text even though the symbol name doesn't contain those words."}
{"id": "symbol_lookup_login", "query": "login function", "language": null, "since_unix": null, "expected_commit_labels": ["login_commit"], "expected_paths": ["src/auth.rs"], "expected_profile": "unknown", "notes": "Bare symbol-name lookup with no language filter — exercises the FTS5-by-symbol-name lane in isolation. Rank-1 must be the commit that introduced `login`, not unrelated `auth`-area touches."}
{"id": "config_query_intent_smoke", "query": "where did we configure the database url", "language": "python", "since_unix": null, "expected_commit_labels": ["config_loader_commit"], "expected_paths": ["app/config.py"], "expected_profile": "configuration", "notes": "Plan 12 Task 2.2: configuration-intent phrasing. The parser should classify this as Configuration and the runner reports a profile mismatch (informational, not gating) if a future change moves the classification."}
{"id": "bug_fix_intent_smoke", "query": "how did we fix the timeout bug", "language": "rust", "since_unix": null, "expected_commit_labels": ["timeout_commit"], "expected_paths": ["src/fetch.rs"], "expected_profile": "bug_fix_precedent", "notes": "Plan 12 Task 2.2: bug-fix-intent phrasing. Recency multiplier should bump the timeout commit above older context in real-world repos; in this tiny fixture the only candidate IS the timeout commit, so the assertion is informational."}
{"id": "api_usage_intent_smoke", "query": "how is `login` called", "language": null, "since_unix": null, "expected_commit_labels": ["login_commit"], "expected_paths": ["src/auth.rs"], "expected_profile": "api_usage", "notes": "Plan 12 Task 2.2: API-usage-intent phrasing. The parser should classify this as ApiUsage and pick the wider rerank pool; the symbol-lane lift is what surfaces the login commit."}
