//! Empty crate root. The actual perf benchmarks live as integration
//! tests under `tests/` (one per fixture). Keeping `lib.rs` empty
//! lets cargo recognise the package without exposing any public API.
